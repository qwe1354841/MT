--开服冲榜表单
FormActRankList = {}

function FormActRankList.get_act_ranklist_info_by_key(player,keyname)
	if Act_RankList then
		Act_RankList.get_act_ranklist_info_by_key(player,keyname)
	end
	return ""
end