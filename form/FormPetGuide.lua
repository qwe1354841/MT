--宠物图鉴表单
FormPetGuide = {}

-- function FormPetGuide.GetData(player)
    -- if PetGuide then
		-- PetGuide.GetData(player)
    -- end
    -- return ""
-- end

function FormPetGuide.PetGetWay(player, GuideIndex, PetIndex, GetWayIndex)
    if PetGuide then
		PetGuide.PetGetWay(player, GuideIndex, PetIndex, GetWayIndex)
    end
    return ""
end